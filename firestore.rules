service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow create,delete,update,list: if false;
      allow get: if request.auth.uid == userId;
    }
  }
}
